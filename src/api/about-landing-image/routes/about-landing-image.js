'use strict';

/**
 * about-landing-image router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::about-landing-image.about-landing-image');
