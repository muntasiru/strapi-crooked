'use strict';

/**
 * about-landing-image service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::about-landing-image.about-landing-image');
